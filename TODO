GOAL:
  [_] Handle HEAD requests
  [_] Handle GET requests

TODO:
  [_] Clean up server code
  [x] Parse request
  [_] Receive data from client and parse it
  [_] Respond to requests

  [_] Write my own assert macro??
  [x] Write tests

Suggestions:
  Rename bin to build
  readv
  sendfile

Create server
  socket()
  bind()
  listen()

Listen for requests
  accept()
  recv()

Respond to requests
  send()
