GOAL:
  [_] Handle HEAD requests
  [_] Handle GET requests

TODO:
  [_] Clean up server code
  [_] Parse request line
  [_] Parse request target
  [_] Write my own assert macro??
  [x] Write tests

Suggestions:
  Rename bin to build
  Rename static to www or public?
  readv
  sendfile

Create server
  socket()
  bind()
  listen()

Listen for requests
  accept()
  recv()

Respond to requests
  send()
