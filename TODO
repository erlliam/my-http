GOAL:
  [_] Handle HEAD requests
  [_] Handle GET requests

TODO:
  [_] Clean up server code
  [_] Parse request line
  [_] Write tests
      [_] Choose between "test" and "tests"
  [_] Write my own assert macro??

Suggestions:
  readv
  sendfile

DRAFT
  Create server
    socket()
    bind()
    listen()

  Listen for requests
    accept()
    recv()

  Respond to requests
    send()

  Problem
    accept() and recv() both block
